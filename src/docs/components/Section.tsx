import styled from "styled-components";

import { styles } from "styles";

export const Section = styled.div`
    margin: 0 0 ${styles.span(4)};
`;

export const SectionTitle = styled.h2`
    margin: 0 0 ${styles.span(3)};
`;
